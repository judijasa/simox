# Machine registry: prod machines only.
# This file is private data, kept in the private config repo and copied into
# etc/ as a real file by simox's bin/fetch-private-data via .private-source
# (see doc/system/private-config.md). `pf-deploy.sh` reads the [prod] roster
# (locally, on the deploy machine — `machines.ini` is never shipped to a host);
# the dev-machine `DBUSER` mapping now lives in etc/team.ini (see
# etc/team.ini.template), not here.
#
# [prod] prod deploy targets. Each key is the server's ZeroTier IP (the same
#        address `pf-deploy.sh` uses for SSH). The value is a comma-separated
#        list of `tag[:name]` tokens:
#          named tag   `db:simo0` -> a `db` resource named `simo0`
#          bare tag    `worker`   -> a bare tag data jobs use as their cron
#                                    scope
#        `db` is the framework's built-in tag: a `db:<name>` token names a
#        database and enforces the one-to-one server mapping (the instance
#        itself is provisioned by `ema create`; db-check verifies it via the
#        host's own `mariadb@*` units, not this roster). Every tag doubles as
#        a cron scope: a `#[CronJob]` declares `scope: <tag[:name]>` (or
#        `scope: host` to run on every host), and `cron-manifest --host-tags
#        <tokens>` installs only jobs whose scope matches this host's tokens —
#        a `host`-scoped job runs on every host. `worker` is an ordinary bare
#        tag in that matcher, nothing special-cases it. Every other tag is
#        consumer-owned:
#          `web`     restore Apache www-data traversal on the repo dir and
#                    install the nix-built php-fpm (pool config + systemd
#                    unit); also opens 80/443/tcp in the firewall (gen-firewall)
#          `cloud`   opens 9993/udp in the firewall (gen-firewall), gated on
#                    the runtime cloud test
#        A host with a `db:<name>` token is a database host; a bare `worker`
#        token scopes data jobs to that host; an empty value is a code-only
#        server (it deploys the codebase but runs no tagged step).
#
#        Service-account pins — the framework `gen-service-accounts` CLI
#        reconciles passwordless host-pinned accounts and roles from the
#        shared srv/roles-<GUID> declaration plus etc/team.ini (the `member`
#        source) and this roster. Only `worker` and `web` are role-pin
#        sources: `worker` -> `simox_worker`, `web` -> `simox_web`. A
#        `db:<name>` tag is a provisioning marker only (which instance a host
#        stores) — it pins no role, since access follows what a host runs
#        (`worker`/`web`) or its members, never what it stores (see
#        doc/system/service-accounts.md). Removing a tag (or a whole host)
#        here revokes the corresponding role on the next reconcile; the
#        `member` IPs come from etc/team.ini, not here.
#
#        Firewall — the framework `gen-firewall` CLI reconciles the ufw rule
#        set from etc/host-hardening.php plus this roster. Tags only ADD allow
#        rules; the baseline (default deny
#        incoming / allow outgoing, SSH 22/tcp from the ZeroTier range) and
#        the built-in `db:<name>` port (resolved from etc/reuter.ini) are
#        framework-owned and never listed here. `web` opens 80/443/tcp;
#        `cloud` opens 9993/udp only when the runtime cloud test also passes
#        (fail-safe: no response -> no 9993).
#
#        Constraint: a named token may appear on exactly ONE server
#        (`db:simo0` and `db:simo1` are independent databases; `web` is
#        independent of both). Bare flags carry no name, hence no uniqueness
#        constraint. A server may host no database (code-only), one database,
#        or several databases (one MariaDB instance each, created by
#        `ema create`).

[prod]
# 10.147.x.10=db:simo0, worker
# 10.147.x.11=db:simo1, web
